<form method="post" action="{{ path('app_adoption_case_delete', {'id': adoption_case.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adoption_case.id) }}">
    <button class="btn w-full bg-red-500 hover:bg-red-400 text-white">Usuń</button>
</form>
