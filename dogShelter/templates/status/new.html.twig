{% extends 'base.html.twig' %}

{% block title %}New Status{% endblock %}

{% block body %}
    <div class="flex-col text-center mt-10 w-[400px] border-1 shadow-xl mx-auto px-2 min-w-[300px]"> 
    <div class="mb-2 text-xl  ">Dodaj Nowy Status</div>
    {{ include('status/_form.html.twig') }}

    <a href="{{ path('app_status_index') }}"><div class="border-x-2 border-b-2 bg-yellow-500 hover:bg-yellow-400 text-white">Wszystkie Posty</div></a>
</div>

    {# {{ include('status/_form.html.twig') }}

    <a href="{{ path('app_status_index') }}">back to list</a> #}
{% endblock %}
