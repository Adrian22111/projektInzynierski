{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Edit User</h1>
    {{ include('user/_form.html.twig', {'button_label': 'Update', 'isEdit' : 'isEdit'}) }}

    <a href="{{ path('app_user_index') }}"><button class="btn">Powrót do listy</button></a>
    <a href="{{ path('app_user_change_password',{'id':user.id}) }}"><button class="btn">Zmień Hasło</button></a>
    {{ include('user/_delete_form.html.twig') }}

{% endblock %}
